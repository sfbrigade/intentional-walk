'use strict'

import {StyleSheet} from 'react-native';
import {Colors} from './colors';

export default StyleSheet.create({
  rounded: {
    borderRadius: 10,
  },
  content: {
    margin: 16,
  }
});
